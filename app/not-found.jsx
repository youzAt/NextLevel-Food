const NotFoundPage = () => {
	return (
		<main className="not-found">
			<h1>not found</h1>
			<p>
				Unfortunately, we could not find the reqeusted page or resource!
			</p>
		</main>
	);
};

export default NotFoundPage;
